import React from 'react';

export const metadata = {
  title: 'Why Choose Us | Find A Meeting Spot',
  description: 'Learn why you should choose Find A Meeting Spot for all your meeting coordination needs.',
};

export default function WhyChooseUsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
} 